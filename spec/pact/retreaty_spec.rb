require 'spec_helper'

describe Pact::Retreaty do
  it 'has a version number' do
    expect(Pact::Retreaty::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
